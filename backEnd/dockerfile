FROM node:alpine as base
WORKDIR /backend
copy package*json ./
RUN rm -rf node_modules && yarn install --frozen-lockfile && yarn cache clean
COPY . .
RUN ls -la
EXPOSE 3000
CMD ["node", "./server.js"]